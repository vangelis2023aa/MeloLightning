name: Build IPA

# Manually-triggered build of the CURRENT MeloLightning source into an
# UNSIGNED iOS/iPadOS .ipa for sideloading (used to test the Task 1
# Skylanders .sky persistence fix). No Apple signing, no secrets.
#
# Pipeline (mirrors how the repo's own iOS app is assembled):
#   1. CMake (Xcode generator, CMAKE_SYSTEM_NAME=iOS, BUILD_HEADLESS_DYLIB=ON)
#      generates build/src/cemuMain.xcodeproj with the CemuBin (-> Cemu.framework)
#      and CemuInput (-> libCemuInput.a) targets and installs the arm64-ios
#      vcpkg dependencies.
#   2. xcodebuild builds the generated core project's CemuBin (-> Cemu.framework)
#      and CemuInput (-> libCemuInput.a) into a shared DerivedData products dir.
#   3. xcodebuild builds MeloCafe/MeloCafe.xcodeproj (scheme "MeloCafe") against
#      that same DerivedData, linking Cemu.framework + libCemuInput.a, producing
#      MeloCafe.app.
#   4. The .app is wrapped as Payload/MeloCafe.app and zipped to MeloLightning.ipa.
#
# It never builds the desktop macOS Cemu target and never installs wxWidgets;
# it is fully independent of the Linux/Windows/macOS/AppImage jobs.

on:
  workflow_dispatch:
    inputs:
      configuration:
        description: "Xcode build configuration"
        type: choice
        default: Release
        options: [Release, Debug]

permissions:
  contents: read

jobs:
  build-ipa:
    # The app uses iOS 27 SDK symbols (UISceneAccessoryRegistration,
    # registerSceneAccessory, .externalNonInteractive) that exist only in the
    # iOS 27 SDK. The macos-26 image ships only up to iOS 26.5, so we need
    # GitHub's Xcode 27 public-preview image, which ships Xcode 27.0 + the
    # iOS 27.0 SDK. (Preview image: may be less stable / have queue delays.)
    runs-on: xcode-27

    env:
      CONFIG: ${{ inputs.configuration || 'Release' }}
      DERIVED: ${{ github.workspace }}/DerivedData
      # Speed up repeat runs by caching compiled vcpkg ports in the GitHub
      # Actions cache. Safe/no-op on the first run.
      VCPKG_BINARY_SOURCES: "clear;x-gha,readwrite"

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          # vcpkg submodule must be a full (non-shallow) clone; the repo's
          # CMake unshallows it, but fetch full history to be safe.
          fetch-depth: 0

      # Exposes the two env vars vcpkg's x-gha binary cache backend needs.
      - name: Enable vcpkg GitHub Actions binary cache
        uses: actions/github-script@v7
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Select Xcode
        run: |
          set -euo pipefail
          # The xcode-27 image ships Xcode 27 (with the iOS 27 SDK) as default.
          # Prefer an Xcode 27.x; fall back to /Applications/Xcode.app (which
          # points at 27 on this image), then to the latest available.
          XC=$(ls -d /Applications/Xcode_27*.app 2>/dev/null | sort -V | tail -n1 || true)
          if [ -z "${XC}" ] && [ -d /Applications/Xcode.app ]; then XC="/Applications/Xcode.app"; fi
          if [ -z "${XC}" ]; then XC=$(ls -d /Applications/Xcode*.app | sort -V | tail -n1); fi
          echo "Using Xcode at: ${XC}"
          sudo xcode-select -s "${XC}/Contents/Developer"

      - name: Toolchain versions
        run: |
          set -euo pipefail
          echo "===== Xcode ====="; xcodebuild -version
          echo "===== SDKs ====="; xcodebuild -showsdks | grep -i ios || true
          echo "===== CMake ====="; cmake --version
          echo "===== clang ====="; clang --version

      - name: Install build tools
        run: |
          set -euo pipefail
          # Tools some vcpkg ports need when built from source for arm64-ios.
          brew install autoconf automake autoconf-archive libtool nasm pkg-config cmake ninja || true

      - name: Bootstrap vcpkg
        run: |
          set -euo pipefail
          ./dependencies/vcpkg/bootstrap-vcpkg.sh

      # ---- Stage 1: CMake configure (vcpkg install + generate Xcode project) ----
      - name: "Stage 1 — CMake configure (iOS, Xcode generator)"
        id: cmake
        run: |
          set -euo pipefail
          # CMAKE_OSX_ARCHITECTURES must be set explicitly: with the Xcode
          # generator + CMAKE_SYSTEM_NAME=iOS, CMake leaves it empty (Xcode
          # resolves ARCHS at build time) and CMAKE_SYSTEM_PROCESSOR is also
          # empty when cross-compiling. dependencies/ih264d/CMakeLists.txt reads
          # CMAKE_OSX_ARCHITECTURES (falling back to CMAKE_SYSTEM_PROCESSOR) and
          # FATAL_ERRORs on an empty value. arm64 is the only arch the arm64-ios
          # vcpkg triplet targets; this mirrors the macOS build (build.yml).
          cmake -S . -B build -G Xcode \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DBUILD_HEADLESS_DYLIB=ON \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=15.0 \
            2>&1 | tee cmake-configure.log
          echo "----- generated Xcode projects -----"
          find build -maxdepth 2 -name "*.xcodeproj" -print
          echo "----- targets in generated core project -----"
          xcodebuild -list -project build/src/cemuMain.xcodeproj 2>&1 | tee cmake-targets.log || true
          test -d build/src/cemuMain.xcodeproj || { echo "::error::CMake did not generate build/src/cemuMain.xcodeproj (Stage: CMake configuration / vcpkg)"; exit 1; }

      # ---- Stage 2: build the generated iOS Cemu core project ----
      # Build the CMake-generated build/src/cemuMain.xcodeproj so that
      # Cemu.framework and libCemuInput.a exist in the SHARED DerivedData
      # products dir that the MeloCafe app project links against. Using the
      # same -derivedDataPath as the app build below is what makes the app's
      # BUILT_PRODUCTS_DIR cross-project references resolve. This step alone
      # isolates C++ compilation / framework-link failures from app failures.
      # Only the iOS CemuBin/CemuInput targets are built — never the desktop
      # macOS Cemu target.
      - name: "Stage 2 — Build iOS Cemu core (Cemu.framework + libCemuInput.a)"
        id: core
        run: |
          set -euo pipefail
          : > core-build.log
          # ih264d, zarchive and xbyak_aarch64 are added with EXCLUDE_FROM_ALL
          # (root CMakeLists.txt), so they are not part of ALL_BUILD and an
          # auto-created single-target scheme build of CemuBin does NOT build
          # them. CemuBin's generated link line references them at their CMake
          # binary-dir output paths (build/lib/Release/libxbyak_aarch64.a,
          # build/dependencies/ih264d/${CONFIG}-iphoneos/libih264d.a,
          # build/dependencies/ZArchive/${CONFIG}-iphoneos/libzarchive.a), so
          # build these targets directly with cmake --build (no -derivedDataPath,
          # so CMake writes them to exactly those binary-dir paths) rather than
          # copying artifacts. The internal Cemu* libraries are in ALL and are
          # already built as ordinary scheme dependencies.
          echo "===== cmake --build ih264d zarchive xbyak_aarch64 =====" | tee -a core-build.log
          cmake --build build --config "${CONFIG}" --target ih264d zarchive xbyak_aarch64 2>&1 | tee -a core-build.log
          # xcodebuild rejects -target together with -derivedDataPath ("-scheme
          # ... is required when specifying -derivedDataPath"). The CMake Xcode
          # generator names one scheme per target, so build the core products by
          # scheme into the shared DerivedData the app build links against.
          # CemuInput (a dependency of CemuBin) is built first so libCemuInput.a
          # is present.
          set +e
          rc=0
          for SCHEME in CemuInput CemuBin; do
            echo "===== xcodebuild -scheme ${SCHEME} =====" | tee -a core-build.log
            xcodebuild \
              -project build/src/cemuMain.xcodeproj \
              -scheme "${SCHEME}" \
              -configuration "${CONFIG}" \
              -sdk iphoneos \
              -destination 'generic/platform=iOS' \
              -derivedDataPath "${DERIVED}" \
              ONLY_ACTIVE_ARCH=NO \
              CODE_SIGN_IDENTITY="" \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGNING_ALLOWED=NO \
              CODE_SIGN_STYLE=Manual \
              DEVELOPMENT_TEAM="" \
              build 2>&1 | tee -a core-build.log
            rc=${PIPESTATUS[0]}
            [ "${rc}" -ne 0 ] && break
          done
          set -e
          if [ "${rc}" -ne 0 ]; then
            echo "::group::Core failure classification"
            if grep -qiE "clang: error|\.(cpp|mm|h):[0-9]+:[0-9]+: error|fatal error: '.*' file not found" core-build.log; then
              echo "::error::Core build failed during C++ compilation of the Cemu core (Stage c)."
            fi
            if grep -qiE "Undefined symbols|ld: |framework not found|library not found|linker command failed" core-build.log; then
              echo "::error::Core build failed during framework/library linking (Stage d)."
            fi
            echo "::endgroup::"
            exit "${rc}"
          fi
          # CMake's Xcode generator bakes its own SYMROOT / CONFIGURATION_BUILD_DIR
          # (the CMake binary dir) into build/src/cemuMain.xcodeproj, so
          # -derivedDataPath does NOT relocate the core products: xcodebuild
          # writes the real Cemu.framework to build/src/${CONFIG}-iphoneos, not
          # ${DERIVED}/Build/Products. Use that explicit CMake output path — a
          # blind `find build -name Cemu.framework` also matches Xcode's eager
          # linking stub at build/build/EagerLinkingTBDs/${CONFIG}-iphoneos/
          # Cemu.framework, which holds only Cemu.tbd (no Mach-O) and was being
          # selected first, producing a false "has no Mach-O executable" failure.
          echo "----- locating core products under build/ -----"
          CEMU_FW_DIR="${GITHUB_WORKSPACE}/build/src/${CONFIG}-iphoneos"
          test -d "${CEMU_FW_DIR}/Cemu.framework" || { echo "::error::Cemu.framework was not produced at ${CEMU_FW_DIR} (Stage d: framework)."; exit 1; }
          test -f "${CEMU_FW_DIR}/Cemu.framework/Cemu" || { echo "::error::Cemu.framework has no Mach-O executable at ${CEMU_FW_DIR} (Stage d: framework)."; exit 1; }
          file "${CEMU_FW_DIR}/Cemu.framework/Cemu" | grep -qiE "Mach-O|dynamically linked shared library|current ar archive|object" \
            || { echo "::error::${CEMU_FW_DIR}/Cemu.framework/Cemu is not a Mach-O binary (Stage d: framework)."; file "${CEMU_FW_DIR}/Cemu.framework/Cemu"; exit 1; }
          # Static lib: locate the real CMake output, excluding the EagerLinkingTBDs
          # stub tree so a .tbd/reference path can never be selected.
          LIB=$(find build/src -type f -name "libCemuInput.a" -path "*${CONFIG}-iphoneos*" ! -path "*EagerLinkingTBDs*" 2>/dev/null | head -n1 || true)
          [ -z "${LIB}" ] && LIB=$(find build/src -type f -name "libCemuInput.a" ! -path "*EagerLinkingTBDs*" 2>/dev/null | head -n1 || true)
          [ -z "${LIB}" ] && LIB=$(find build -type f -name "libCemuInput.a" ! -path "*EagerLinkingTBDs*" 2>/dev/null | head -n1 || true)
          test -n "${LIB}" && test -f "${LIB}" || { echo "::error::libCemuInput.a was not produced by the core build (Stage d: static lib)."; exit 1; }
          CEMU_LIB_DIR=$(cd "$(dirname "${LIB}")" && pwd)
          echo "Selected Cemu.framework dir: ${CEMU_FW_DIR}"
          echo "Selected libCemuInput.a dir: ${CEMU_LIB_DIR}"
          ls -l "${CEMU_FW_DIR}/Cemu.framework"
          file "${CEMU_FW_DIR}/Cemu.framework/Cemu"
          ls -l "${CEMU_LIB_DIR}/libCemuInput.a"
          # Hand the real product directories to the app build below.
          { echo "CEMU_FW_DIR=${CEMU_FW_DIR}"; echo "CEMU_LIB_DIR=${CEMU_LIB_DIR}"; } >> "$GITHUB_ENV"

      # ---- Stages 3-4: build the MeloCafe app (links the core, unsigned) ----
      - name: "Stages 3-4 — Build MeloCafe app (iOS device, unsigned)"
        id: xcodebuild
        run: |
          set -euo pipefail
          set +e
          # The core products live under build/ (CMake's Xcode generator bakes
          # its own build dir), NOT in ${DERIVED}/Build/Products. Point the app's
          # linker at the directories Stage 2 discovered (CEMU_FW_DIR / CEMU_LIB_DIR)
          # so Cemu.framework and libCemuInput.a resolve for the link step; the
          # cross-project reference proxies in MeloCafe.xcodeproj resolve the embed
          # from the same core build. $(inherited) is preserved so the project's
          # own FRAMEWORK_SEARCH_PATHS / LIBRARY_SEARCH_PATHS are not lost.
          echo "Using core products: framework=${CEMU_FW_DIR:-<unset>} lib=${CEMU_LIB_DIR:-<unset>}"
          xcodebuild \
            -project MeloCafe/MeloCafe.xcodeproj \
            -scheme MeloCafe \
            -configuration "${CONFIG}" \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "${DERIVED}" \
            -skipMacroValidation \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM="" \
            FRAMEWORK_SEARCH_PATHS="\$(inherited) ${CEMU_FW_DIR}" \
            LIBRARY_SEARCH_PATHS="\$(inherited) ${CEMU_LIB_DIR}" \
            build 2>&1 | tee xcodebuild.log
          rc=${PIPESTATUS[0]}
          set -e
          if [ "${rc}" -ne 0 ]; then
            echo "::group::App failure classification"
            if grep -qiE "Undefined symbols|ld: |framework not found|library not found|linker command failed" xcodebuild.log; then
              echo "::error::App build failed during framework/library linking (Stage d)."
            fi
            if grep -qiE "\.swift:[0-9]+:[0-9]+: error|Swift Compiler Error|bridging header" xcodebuild.log; then
              echo "::error::App build failed during iOS app (Swift) compilation (Stage e)."
            fi
            if grep -qiE "unable to resolve package|package resolution|Melo-Controller" xcodebuild.log; then
              echo "::error::App build failed resolving the Melo-Controller Swift package (network/SPM)."
            fi
            echo "::endgroup::"
            exit "${rc}"
          fi

      # ---- Stage 5: locate .app and package the unsigned IPA ----
      - name: "Stage 5 — Package unsigned IPA"
        id: package
        run: |
          set -euo pipefail
          PRODUCTS="${DERIVED}/Build/Products/${CONFIG}-iphoneos"
          echo "Searching for MeloCafe.app under: ${PRODUCTS}"
          APP="${PRODUCTS}/MeloCafe.app"
          if [ ! -d "${APP}" ]; then
            APP=$(find "${DERIVED}/Build/Products" -maxdepth 2 -name "MeloCafe.app" -type d | head -n1 || true)
          fi
          test -n "${APP}" && test -d "${APP}" || { echo "::error::MeloCafe.app was not produced (Stage: packaging)"; exit 1; }
          echo "Found .app at: ${APP}"
          # A real Mach-O executable must exist inside the bundle.
          test -f "${APP}/MeloCafe" || { echo "::error::MeloCafe.app has no main executable — build did not actually produce the app."; exit 1; }
          file "${APP}/MeloCafe"

          rm -rf Payload MeloLightning.ipa
          mkdir -p Payload
          cp -R "${APP}" "Payload/MeloCafe.app"
          # -y preserves symlinks inside frameworks (required for a valid bundle).
          /usr/bin/zip -qry MeloLightning.ipa Payload
          echo "----- IPA contents (top level) -----"
          unzip -l MeloLightning.ipa | head -n 20
          test -s MeloLightning.ipa || { echo "::error::MeloLightning.ipa is empty (Stage: packaging)"; exit 1; }
          echo "app_path=${APP}" >> "$GITHUB_OUTPUT"

      - name: Upload unsigned IPA
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: MeloLightning-unsigned-ipa
          path: MeloLightning.ipa
          if-no-files-found: error

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            cmake-configure.log
            cmake-targets.log
            core-build.log
            xcodebuild.log
          if-no-files-found: warn
